export const DEFAULT_CONFIG = {
  TITLE:      "Top Languages",
  WIDTH:      400,
  MIN_WIDTH:  400,
  HEIGHT:     300,
  MIN_HEIGHT: 265,
  COUNT:      8,
  MAX_COUNT:  16
} as const;

export const PARAM_DEFAULTS = {
  THEME:    "default",
  TYPE:     "donut",
  GAP_TYPE: "gap",
} as const;
